#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @x: integer
 * Return: the absolute val of integer
 */
int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
